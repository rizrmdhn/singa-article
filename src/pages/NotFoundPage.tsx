import { NotFoundComponent } from "@/components/component/not-found-component";

export default function NotFoundPage() {
  return (
    <div className="flex min-h-[66vh] w-full flex-col items-center justify-center gap-4 text-center">
      <NotFoundComponent />
    </div>
  );
}
